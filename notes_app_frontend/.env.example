# Supabase (required for using Supabase backend; otherwise app uses localStorage)
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=

# Optional site URL (future auth flows, redirects, etc.)
REACT_APP_SITE_URL=http://localhost:3000/
